import 'package:flutter/material.dart';
import 'package:material_symbols_icons/material_symbols_icons.dart';

class AppIcons {
  static const Icon profileIcon = Icon(Symbols.person);
  static const Icon logoutIcon = Icon(Symbols.logout);
  static const Icon searchIcon = Icon(Symbols.search);
  static const Icon addIcon = Icon(Symbols.add);
  static const Icon minusIcon = Icon(Icons.remove);
}
